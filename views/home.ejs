<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Document</title>
    <link
      href="https://cdn.jsdelivr.net/npm/remixicon@4.5.0/fonts/remixicon.css"
      rel="stylesheet"
    />
  </head>
  <body class="bg-slate-50 text-slate-800 min-h-screen">
    <div id="main" class="px-4 sm:px-6 py-4 sm:py-6 max-w-5xl mx-auto">
      <nav class="flex flex-col md:flex-row items-start md:items-center justify-between gap-3 md:gap-0 bg-white/80 backdrop-blur px-4 py-3 rounded-xl shadow-sm border border-slate-200">
        <div id="left">
          <h2 class="bg-blue-600 py-1 px-1 rounded-md text-xl text-white">Khatabook</h2>
        </div>
        <div id="right" class="flex gap-3 md:gap-5 flex-wrap items-center">
          <a href="/" class="text-slate-600 hover:text-blue-600 font-medium transition">Home</a>
          <a href="/create" class="text-slate-600 hover:text-blue-600 font-medium transition">CreateNewHisaab by Name</a>
          <a href="/createByTime" class="text-slate-600 hover:text-blue-600 font-medium transition">CreateNewHisaab by time</a>
          <% if (currentUser) { %>
            <form action="/logout" method="post"><button class="text-slate-600 hover:text-red-600 font-medium transition">Logout (<%= currentUser.name %>)</button></form>
          <% } else { %>
            <a href="/login" class="text-slate-600 hover:text-blue-600 font-medium transition">Login</a>
          <% } %>
        </div>
      </nav>
      <div id="center" class="mt-6 sm:mt-10">
        <h1 class="text-2xl sm:text-3xl font-semibold">All Hisaab Kitaab</h1>
        <% if(hisaabs.length > 0) { %>
          <% hisaabs.forEach(function(doc){ %>
            <div
            class="hisaab flex flex-col sm:flex-row justify-between items-start sm:items-center gap-3 sm:gap-0 bg-white rounded-xl border border-slate-200 shadow-sm px-4 py-3 mt-4 transition hover:shadow-md"
          >
            <div id="left " class="flex items-center gap-3 flex-wrap">
              <h1 class="text-slate-800 font-medium"><%= doc.title %></h1>
             <a href="/hisaab/<%= doc._id %>"><i class="ri-arrow-right-line text-blue-600 text-lg transition-transform hover:translate-x-0.5"></i></a>
            </div>
            <div id="right" class="flex justify-center items-center gap-2 sm:gap-3">
              <a href="/edit/<%= doc._id %>"><i class="ri-pencil-line text-slate-500 hover:text-blue-600 transition"></i></a>
              <a href="/delete/<%= doc._id %>"><i class="ri-delete-bin-line text-red-500 text-xl hover:scale-105 transition"></i></a>
            </div>
          </div>
          <% }) %>
          <% }  else { %>
            <h1 class="text-xl mt-5 text-slate-500">No Hisaab is created till now...</h1>
         <%  } %>
      </div>
    </div>
  </body>
</html>
